apiVersion: v1
kind: Pod
spec:
  containers:
    - name: node
      image: node:20-alpine
      command:
        - cat
      tty: true
      resources:
        limits:
          memory: "1Gi"
          cpu: "500m"